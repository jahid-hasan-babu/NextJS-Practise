// Using square bracket notation
const numbers: number[] = [1, 2, 3];

// Using array constructor
const numbers1: number[] = new Array(1, 2, 3, 5);

// Using Array.of method
const names: string[] = Array.of("Jahid", "Hasan", "Babu");

// Output the second element from the 'names' array
console.log(names[1]); // Output: "Hasan"
