"noEmitOnError": true,  use korle error hole js a convert hobe na
TypeScript is a superset of JavaScript that adds static typing, enabling developers to catch errors during development and write more reliable, maintainable code. It supports modern JavaScript features and compiles to plain JavaScript, making it compatible with any JS environment
: --> its call taae annotation
when we work dynamic data then use any
if we set type it call type annotation if it get automatically it call inference

for..of --> return value;
for..in --> in means index --> return index;
foreach --> each value
map --> return new array --if we need all data
filter --> if we need only filter data

tuples are a data structure that allows you to store a fixed-size collection of elements of different types.
we use tuple cause in array user that not push any data
